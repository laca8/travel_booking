{
  "scripts": {
    "server": "nodemon server",
    "build": "npm install && cd clients && npm install && npm run build",
    "client": "npm run dev --prefix clients",
    "dev": "concurrently \"npm run server \" \"npm run client\""
  },
  "description": "",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^9.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.9.0",
    "nodemon": "^3.1.7"
  }
}
